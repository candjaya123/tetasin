import 'package:flutter/material.dart';

class AppColors {
  // Brand
  static const primary = Color(0xFFFDB827);
  static const onPrimary = Color(0xFF1A1A1A);

  // Neutrals
  static const black = Color(0xFF111827);
  static const darkGrey = Color(0xFF374151);
  static const mediumGrey = Color(0xFF6B7280);
  static const lightGrey = Color(0xFF9CA3AF);
  static const border = Color(0xFFF3F4F6);
  static const surface = Color(0xFFF9FAFB);
  static const white = Color(0xFFFFFFFF);

  // Semantic
  static const error = Color(0xFFEF4444);
  static const errorLight = Color(0xFFFEF2F2);
  static const success = Color(0xFF10B981);
  static const successLight = Color(0xFFECFDF5);
  static const warning = Color(0xFFF59E0B);
  static const warningLight = Color(0xFFFFFBEB);
  static const info = Color(0xFF3B82F6);

  // Background
  static const background = Color(0xFFF9FAFB);

  // Additional Brand Colors
  static const secondary = Color(0xFF1E293B); // Dark Slate for personal mode
}
